#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{
      long long a, b, sum;
      scanf("%lld %lld", &a, &b);
      sum = a * b;
      printf("%lld", sum);
      return 0;
}